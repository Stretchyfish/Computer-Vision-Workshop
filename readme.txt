These are the files used for the Computer Vision Workshop

Make sure to have a python IDE installed (PyCharm forexample).
Make sure to have a python interpreter installet.

Open terminal (windows cmd)
write: pip install opencv-python

If using PycCharm, download package opencv-python
